apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pruebaweb.fullname" . }}-test-connection"
  labels:
    {{- include "pruebaweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pruebaweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
